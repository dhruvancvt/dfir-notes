# Detections

- `yara/` — YARA rules (e.g. the 3 Infinity External rules; add the files here)
- `iocs/` — MISP event JSON / ThreatFox-style exports. **Keep IOCs defanged in markdown.**

## Sharing IOCs
- **MISP** event JSON imports cleanly into other MISP instances.
- **ThreatFox** — check the licensing/terms before republishing their data.
